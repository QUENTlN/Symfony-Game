{% extends 'base.html.twig' %}

{% block title %}
    Modifier la question
{% endblock %}

{% block body %}
    <main class="page contact-page">
        <section class="portfolio-block contact">
            <div class="container">
                <div class="heading">
                    <h2>Modifier une question</h2>
                </div>
                {{ form_start(form) }}
                <div class="form-group">
                    {{ form_row(form.status,{'label':'Statut :', 'attr':{'class':'form-control '}}) }}
                </div>
                <div class="form-group">
                    {{ form_row(form.text,{'label': 'Text :', 'attr':{'class':'form-control '}}) }}
                </div>
                <div class="form-group">
                    {{ form_row(form.player,{'label':'Joueur :', 'attr':{'class':'form-control  '}}) }}
                </div>
                <div class="form-group">
                    {{ form_row(form.subCategory,{'label':'Sous catégorie :', 'attr':{'class':'form-control'  }}) }}
                </div>
                <br>
                <div class="form-group"
                    ><button class="btn btn-primary btn-block" type="submit">Enregistrez vos modifications de la question</button>
                </div>
                {{ form_end(form) }}
        </section>
    </main>

{% endblock %}