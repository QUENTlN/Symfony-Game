{{ form_start(form) }}
    <div class="form-group">
        {{ form_row(form.nameProfil,{'label':'Nom du profil :', 'attr':{'class':'form-control '}}) }}
    </div>
    <div class="form-group">
        {{ form_row(form.nbMaxPlayer,{'label':'Nombre de joueurs maximum :', 'attr':{'class':'form-control '}}) }}
    </div>
    <div class="form-group">
        {{ form_row(form.showScore,{'label':'Montrer le score :', 'attr':{'class':'form-check-label '}}) }}
    </div>
    <div class="form-group">
        {{ form_row(form.oneAnswerOnly,{'label':'Montrer le score :', 'attr':{'class':'form-check-label '}}) }}
    </div>
    <div class="form-group">
        {{ form_row(form.numberRound,{'label':'Nombre de tours', 'attr':{'class':'form-control '}}) }}
    </div>
        <div class="form-group">
            {#  {{ form_row(form.subCategories,{'label':false, 'attr':{'class':'form-check-label'}}) }} #}
    </div>
        <div>
        <div class="modal-footer"><button class="btn btn-light" type="reset" data-dismiss="modal">Annuler</button><button class="btn btn-primary btn-success" type="submit" >Créer</button></div>
    </div>
    {# <div class="alert alert-danger" > {{ form_rest(form) }}</div>#}
    {{ form_end(form) }}